apiVersion: apps/v1
kind: Deployment
metadata:
  name: amq-plainconsumer-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: amq-plainconsumer
  template:
    metadata:
      labels:
        app: amq-plainconsumer
    spec:
      automountServiceAccountToken: false
      containers:
      - name: amq-plainconsumer
        image: localhost/amq-plainconsumer:1.0.1
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            memory: 100Mi
            ephemeral-storage: "2Gi"
          requests:
            cpu: 0.5
            memory: 100Mi
            ephemeral-storage: "2Gi"
        ports:
        - containerPort: 61616
---
apiVersion: v1
kind: Service
metadata:
  name: amq-plainconsumer-service
spec:
  selector:
    app: amq-plainconsumer
  ports:
  - protocol: TCP
    port: 61616
    targetPort: 61616
  type: ClusterIP
